<div id="<%= dom_id(question) %>" class="question">

  <p class="user-question"><%= question.user_question %></p>
  <p class="ai-answer"><%= sanitize(question.ai_answer) || "Thinking..."%></p>
</div>
