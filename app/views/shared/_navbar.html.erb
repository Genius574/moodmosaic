<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
  <% if controller_name == "moods" && action_name == "new"%>
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag "mood_logo2-removebg-preview.png", alt: "Logo" %>
    <% end %>
  <% else %>
    <%= link_to new_mood_path, class: "navbar-brand" do %>
      <%= image_tag "mood_logo2-removebg-preview.png", alt: "Logo" %>
    <% end %>
  <% end %>


    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <% if controller_name == "pages" && action_name == "home"%>
            <!-- Home page -->
            <li class="nav-item dropdown">
              <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= image_tag "avatar_mood-removebg-preview.png", class: "avatar", alt: "dropdown menu" %>
              </a>
              <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
              </div>
            </li>
          <% elsif controller_name == "moods" && action_name == "new" %>
            <!-- Moods New page(select a feeling) -->
            <li class="nav-item dropdown">
              <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= image_tag "avatar_mood-removebg-preview.png", class: "avatar", alt: "dropdown menu" %>              </a>
              <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
              </div>
            </li>
          <% elsif controller_name == "moods" && action_name == "edit" %>
            <!-- Moods edit page(where a user selects a category) -->
             <li class="nav-item">
               <%= link_to "Change A Feeling?", new_mood_path, class: "nav-link" %>
             </li>
            <li class="nav-item dropdown">
              <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= image_tag "avatar_mood-removebg-preview.png", class: "avatar", alt: "dropdown menu" %>              </a>
              <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
              </div>
            </li>
          <% elsif controller_name == "contents" && action_name == "index" %>
            <!-- Contents index page(where content is shown) -->
            <li class="nav-item">
               <%= link_to "Change A Feeling?", new_mood_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
               <%= link_to "Change A Category?", edit_mood_path(@mood), class: "nav-link" %>
             </li>
            <li class="nav-item dropdown">
              <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= image_tag "avatar_mood-removebg-preview.png", class: "avatar", alt: "dropdown menu" %>
              </a>
              <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
              </div>
            </li>
          <% elsif controller_name == "contents" && action_name == "show" %>
            <!-- One Blog  -->
            <li class="nav-item">
                <%= link_to "Back to all content", contents_path, class: "nav-link" %>
            </li>
            <li class="nav-item dropdown">
              <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= image_tag "avatar_mood-removebg-preview.png", class: "avatar", alt: "dropdown menu" %>
              </a>
              <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
              </div>
            </li>
          <% end %>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
